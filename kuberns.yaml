project: mtn-uganda-proxy
environment: production
resources:
  cpu: 0.5
  memory: 256Mi
autoscaling:
  min: 1
  max: 1
service:
  type: LoadBalancer
  port: 3000
  protocol: TCP
healthCheck:
  path: /status
  port: 3000
  initialDelay: 30
  period: 60
